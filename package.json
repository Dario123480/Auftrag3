{
  "name": "burger-empire-optimierung",
  "version": "1.0.0",
  "description": "Optimierung Task Runner",
  "main": "minify.js",
  "scripts": {
    "clean": "rimraf docs style.css style.css.map",
    "rebuild": "npm run clean && npm install && npm run build",
    "build": "sass style.scss style.css && node minify.js"
  },
  "devDependencies": {
    "clean-css": "^4.2.4",
    "fs-extra": "^11.1.0",
    "html-minifier": "^4.0.0",
    "rimraf": "^5.0.0",
    "sass": "^1.99.0"
  }
}
